# Репозиторий публичный, поэтому список ниже — не про аккуратность, а про то,
# что нельзя выкладывать наружу. Каждая строка с причиной: без неё через месяц
# никто не вспомнит, почему файл исключён, и вернёт его обратно.

# Внутренние материалы: бренд-платформа, исследование цен, концепция, спецификация GUI.
uploads/

# Макет от 08.08, заменённый на radar/. Ни одна страница на него не ссылается, а внутри
# зашиты адреса прокси и имена аккаунтов флота — в публичный репозиторий он не идёт.
/Atomic Radar.dc.html

# Ручные бэкапы: ровно то, что теперь заменяет история git.
*.zip
*.bak/
.thumbnail

node_modules/
.DS_Store
Thumbs.db
