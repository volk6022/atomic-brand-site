# Сайт правится на Windows, а разворачивается на Linux клоном этого же репозитория.
# Без этой строки в историю уезжают то LF, то CRLF, и первый же `git pull` на проде
# показывает изменённым весь файл целиком.
* text=auto eol=lf
